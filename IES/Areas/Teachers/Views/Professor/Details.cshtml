@model Model.Teacher.Professor

@{
    ViewData["Title"] = "Professor";
    Layout = "_Layout";
}
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-area="Teachers" asp-route-id="@Model.ProfessorId" class="btn btn-primary">Edit</a>
    <a asp-action="Index" asp-area="Teachers" class="btn btn-default">Back to List</a>
</div>